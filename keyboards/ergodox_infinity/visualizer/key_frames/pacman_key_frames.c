#include "visualizer.h"
#include "visualizer_keyframes.h"
#include "pacman_key_frames.h"
#include "../bmps/pacman_bmps.h"

bool lcd_keyframe_draw_pacman_00(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_00);

    return false;
};

bool lcd_keyframe_draw_pacman_01(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_01);

    return false;
};

bool lcd_keyframe_draw_pacman_02(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_02);

    return false;
};

bool lcd_keyframe_draw_pacman_03(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_03);

    return false;
};

bool lcd_keyframe_draw_pacman_04(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_04);

    return false;
};

bool lcd_keyframe_draw_pacman_05(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_05);

    return false;
};

bool lcd_keyframe_draw_pacman_06(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_06);

    return false;
};

bool lcd_keyframe_draw_pacman_07(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_07);

    return false;
};

bool lcd_keyframe_draw_pacman_08(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_08);

    return false;
};

bool lcd_keyframe_draw_pacman_09(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_09);

    return false;
};

bool lcd_keyframe_draw_pacman_10(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_10);

    return false;
};

bool lcd_keyframe_draw_pacman_11(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_11);

    return false;
};

bool lcd_keyframe_draw_pacman_12(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_12);

    return false;
};

bool lcd_keyframe_draw_pacman_13(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_13);

    return false;
};

bool lcd_keyframe_draw_pacman_14(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_14);

    return false;
};

bool lcd_keyframe_draw_pacman_15(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_15);

    return false;
};

bool lcd_keyframe_draw_pacman_16(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_16);

    return false;
};

bool lcd_keyframe_draw_pacman_17(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_17);

    return false;
};

bool lcd_keyframe_draw_pacman_18(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_18);

    return false;
};

bool lcd_keyframe_draw_pacman_19(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_19);

    return false;
};

bool lcd_keyframe_draw_pacman_20(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_20);

    return false;
};

bool lcd_keyframe_draw_pacman_21(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_21);

    return false;
};

bool lcd_keyframe_draw_pacman_22(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_22);

    return false;
};

bool lcd_keyframe_draw_pacman_23(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_23);

    return false;
};

bool lcd_keyframe_draw_pacman_24(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_24);

    return false;
};

bool lcd_keyframe_draw_pacman_25(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_25);

    return false;
};

bool lcd_keyframe_draw_pacman_26(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_26);

    return false;
};

bool lcd_keyframe_draw_pacman_27(keyframe_animation_t* animation, visualizer_state_t* state) {
    (void)state;
    gdispClear(White);

    gdispGBlitArea(GDISP, 0, 0, LCD_WIDTH, LCD_HEIGHT, 0, 0, LCD_WIDTH, (pixel_t*)pacman_bmp_27);

    return false;
};

